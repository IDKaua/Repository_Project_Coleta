// Este é o Controller: apenas lógica, sem HTML!
export const handleAccess = (tipo) => {
  console.log(`[CONTROLLER] Acessando perfil: ${tipo}`);
  alert(`Redirecionando para o painel de: ${tipo}`);
  
  // No futuro, aqui você fará o redirecionamento de rotas
};